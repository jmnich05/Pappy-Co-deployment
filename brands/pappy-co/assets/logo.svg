<!-- Pappy & Co brand policy: never use a recreated or approximated keys mark.
     Only the official asset at brands/pappy-co/assets/logo.png is permitted.
     content.json points at logo.png; this file is intentionally empty so even
     if a future code path references logo.svg, no recreated mark renders. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1 1" width="1" height="1" aria-hidden="true"></svg>
